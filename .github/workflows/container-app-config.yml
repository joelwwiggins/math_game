properties:
  managedEnvironmentId: /subscriptions/990b7133-9bc1-4da1-9836-8b8a4897475f/resourceGroups/mathgameresource/providers/Microsoft.App/managedEnvironments/mathgameeditor
  configuration:
    ingress:
      external: true
      targetPort: 80
    registries:
      - server: mathgameregistry.azurecr.io
        username: mathgameregistry
    template:
      containers:
        - image: mathgameregistry.azurecr.io/mathgamecontainer:${{ github.sha }}
          name: mathgamecontainer
          env:
            - name: DOTNET_ENVIRONMENT
              value: Production
            - name: WEBSITES_PORT
              value: "80"
            - name: AZURE_STORAGE_ACCOUNT_NAME
              value: "${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}"
            - name: AZURE_STORAGE_ACCOUNT_KEY
              value: "${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}"
            - name: FILE_SHARE_NAME
              value: "${{ secrets.FILE_SHARE_NAME }}"
          volumeMounts:
            - mountPath: /mnt/azure
              volumeName: azure-file-share
      scale:
        minReplicas: 1
        maxReplicas: 1
      volumes:
        - name: azure-file-share
          azureFile:
            shareName: "${{ secrets.FILE_SHARE_NAME }}"
            storageAccountName: "${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}"
            storageAccountKey: "${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}"
